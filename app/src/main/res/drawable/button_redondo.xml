<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Círculo externo (esfera completa) -->
    <item >
        <shape android:shape="rectangle">
            <solid android:color="@color/background_button" />
            <corners android:radius="500dp" />
        </shape>
    </item>

</layer-list>
